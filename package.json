{
  "name": "sugarshin.net",
  "version": "1.0.0",
  "description": "sugarshin.net",
  "private": true,
  "scripts": {
    "clean:.nuxt": "if [ -d .nuxt ]; then rm -rf .nuxt; fi",
    "clean:build": "if [ -d build ]; then rm -rf build; fi",
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "pregenerate": "npm run clean:.nuxt && npm run clean:build",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint",
    "test": "echo TBD.."
  },
  "repository": "git@github.com:sugarshin/sugarshin.net.git",
  "author": "Shingo Sato <shinsugar@gmail.com>",
  "license": "UNLICENSED"
}
